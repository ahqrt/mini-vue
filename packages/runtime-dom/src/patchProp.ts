/**
 * 对于dom属性的更新
 * @param el 元素
 * @param key 属性
 * @param preValue 前一个值 
 * @param nextValue 后一个值
 */
export function patchProp(el, key, preValue, nextValue) {
  // 类名处理

  // 样式处理

  // 事件处理

  // 普通属性
}